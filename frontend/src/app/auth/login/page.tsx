import LoginForm from "./form";

const Page = async () => {
  return (
    <div className="h-full w-full flex items-center justify-center">
      <LoginForm />
    </div>
  );
};

export default Page;
