import RegisterForm from "./form";

const Page = async () => {
  return (
    <div className="h-full w-full flex items-center justify-center">
      <RegisterForm />
    </div>
  );
};

export default Page;
